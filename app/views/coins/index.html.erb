<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Coins</title>
  <%= stylesheet_link_tag "coins", "data-turbo-track": "reload" %>
</head>
<body>
  <div class="container-coin">
    <main>
      <h1> <%= t('coin-title') %> </h1>
      <table class="table-one-coin">
        <thead>
          <tr>
            <th><p><%= @coins.human_attribute_name(:url_image) %></p></th>
            <th><p><%= @coins.human_attribute_name(:description) %></p></th>
            <th><p><%= @coins.human_attribute_name(:acronym) %></p></th>
            <th><p> <%= t('cotation-title') %> </p></th>
            <th><p> <%= t('value-title') %> </p></th>
          </tr>
        </thead>

        <tbody class="table-body-coin">
          <% @coins.each do |coin| %>
            <tr>
              <td><%= image_tag coin.url_image, style: 'height:45px; width:45px;' %></td>
              <td><%= coin.description %></td>
              <td><%= coin.acronym %></td>
              <td>R$100.000,00</td>
              <td>R$300.000,00</td>
              <td><%= link_to t('links.show'), coin, class:"btn btn-lg" %></td>
              <td><%= link_to t('links.edit'), edit_coin_path(coin), class:"btn btn-lg" %></td>
              <td><%= link_to t('links.delete'), coin, class:"btn btn-lg", method: :delete, data: { confirm: t('messages.confirm')} %></td>
            </tr>
          <% end %>
        </tbody>
        
        <div class="new-coin">
          <%= link_to t('new.coin'), new_coin_path, class:"btn btn-lg" %>
        </div>

      </table>
    </main>
  </div>
</body>
</html>
